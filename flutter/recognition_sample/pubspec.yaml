name: recognition_sample
description: id3 Face SDK recognition sample.

publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  id3_face:
    path: ../../sdk/dart/id3_face

  camera: ^0.10.5+2 
  path_provider: ^2.0.15

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1

flutter:
  uses-material-design: true
  assets:
    - assets/models/
